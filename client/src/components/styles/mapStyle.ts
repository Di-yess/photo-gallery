const mapStyle = {
  mapForm: {
    bottom: '8vh',
    right: '1vh',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    position: 'absolute',
    borderRadius: '10px',
    overflow: 'hidden',
  },
};

export default mapStyle;
